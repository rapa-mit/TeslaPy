# Archived previous-vehicle data

Contents moved here on 2026-09-07 during the switch to the new
2026 Tesla Model Y (VIN 7SAYGDED4TA707626).

- `charge_history.json`   - charge history captured from the previous vehicle
                            (was `test.json` in the repo root)
- `Model_Y.json`          - old/malformed nominal-data variant (invalid JSON;
                            the working copy is `../Model_Y.yaml`)
- `history.py`            - old ad-hoc script: dumped vehicle + charge history
- `test.py`              - old ad-hoc script: speed monitor -> Slack webhook
- `cache.json.bak-*`      - backup of the auth token cache (EXPIRED tokens)
- `*~`                    - editor autosave backups

These are kept for reference only. The current app is `../app.py`.
